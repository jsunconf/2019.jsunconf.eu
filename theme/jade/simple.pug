extends _layout

block header
  include index-partials/header

block main
  section.section
    header.section__header
      h1 !{title}
      if exerpt
        p.exerpt !{exerpt}

    .section__content !{contents}
